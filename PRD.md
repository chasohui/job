# PRD — 직무 역량 및 준비 순서 추천 서비스

## 1) 목표와 성공조건

### 목표
취업 준비생이 **전공, 희망 직무, 현재 준비 상황**을 입력하면 AI가 이를 분석하여 해당 직무에 필요한 역량과 사용자가 보완해야 할 역량을 알려주고, **무엇을 어떤 순서로 준비해야 하는지** 추천한다.

### 성공조건
- 입력 완료 후 **20초 이내 AI 분석 결과**가 표시된다.
- 결과에 **핵심 역량, 부족한 역량, 추천 준비 항목**이 모두 표시된다.
- 핵심 역량은 **5~7개 내외**로 제공한다.
- 추천 준비 항목은 **최소 3개 이상** 제공한다.
- 각 추천 항목에 **우선순위/단계, 추천 이유, 구체적인 준비 방법, 다음 행동**이 포함된다.
- 준비 항목을 **1단계 → 2단계 → 3단계** 순서로 확인할 수 있다.
- 오류가 발생해도 입력 내용이 사라지지 않는다.
- 결과를 다시 분석할 수 있다.
- 로그인, 결제, DB, 외부 서비스 연동은 구현하지 않는다.

---

## 2) 사용자 스토리

### 사용자 스토리 1 — 필요한 역량 확인
> 취업 준비생으로서 내가 희망하는 직무에 필요한 핵심 역량을 빠르게 파악하고 싶다. 여러 사이트를 돌아다니며 정보를 수집할 시간이 부족하기 때문이다.

### 사용자 스토리 2 — 부족한 역량 확인
> 희망 직무가 있는 취업 준비생으로서 현재 내가 부족한 역량이 무엇인지 알고 싶다. 내가 어떤 부분을 더 준비해야 하는지 판단하기 어렵기 때문이다.

### 사용자 스토리 3 — 준비 순서 확인
> 취업 준비생으로서 준비해야 할 항목을 우선순위와 순서대로 확인하고 싶다. 무엇부터 준비해야 할지 판단하기 어렵기 때문이다.

### 사용자 스토리 4 — 바로 행동하기
> 취업 준비생으로서 추천받은 역량을 실제로 어떻게 준비해야 하는지도 알고 싶다. 단순히 역량 이름만 알려주면 무엇을 해야 할지 알기 어렵기 때문이다.

---

## 3) 화면 명세

### 3.1 화면 구성
전체 서비스는 **4개 내외의 화면 흐름**으로 구성한다.

1. 입력 화면
2. 준비 상황 확인 화면
3. 분석 중 화면
4. 결과 화면

로그인, 회원가입, 결제, 저장 기능을 별도 화면으로 만들지 않는다.

### 3.2 입력 화면
사용자가 직무 분석에 필요한 정보를 입력한다.

| 항목 | 입력 방식 | 목적 |
|---|---|---|
| 전공 | 한 줄 텍스트 입력 | 사용자의 전공 정보 확인 |
| 희망 직무 | 한 줄 텍스트 입력 | 분석 대상 직무 확인 |
| 현재 준비 상황 | 여러 줄 텍스트 입력 | 현재 역량 및 준비 수준 파악 |

안내 문구:
- 전공: `전공을 입력하세요`
- 희망 직무: `희망 직무를 입력하세요`
- 현재 준비 상황: `현재까지 준비한 내용을 입력하세요`

버튼:
- `다음` 또는 `분석하기`
- 정상 입력이면 다음 단계로 이동한다.
- 유효하지 않으면 해당 필드에 오류 메시지를 표시한다.

### 3.3 준비 상황 확인 화면
사용자가 입력한 내용을 분석 전에 확인한다.

표시:
- 전공
- 희망 직무
- 현재 준비 상황

버튼:
- `수정하기`
- `분석 시작`

### 3.4 분석 중 화면
AI 분석 진행 중 표시한다.

- 로딩 상태
- `현재 준비 상황을 분석하고 있어요.`
- `직무에 필요한 역량과 준비 순서를 정리하고 있어요.`

분석 중에는 중복 요청을 방지하기 위해 분석 시작 버튼을 비활성화한다.

### 3.5 결과 화면
AI 분석 완료 후 다음 순서로 표시한다.

#### ① 핵심 역량
- 해당 직무에 필요한 핵심 역량을 **5~7개 내외**로 표시한다.
- 각 역량에 간단한 설명을 제공한다.

#### ② 부족한 역량
- 현재 준비 상황과 직무 요구 역량을 비교해 보완이 필요한 역량을 표시한다.
- 각 항목에 부족하다고 판단한 이유를 표시한다.

#### ③ 추천 준비 항목
최소 3개 이상의 준비 항목을 우선순위 순서로 표시한다.

각 항목:
- 단계/우선순위
- 준비 항목명
- 추천 이유
- 구체적인 준비 방법
- 다음 행동

#### ④ 다시 추천
`다시 분석하기` 버튼을 제공한다.
- 기존 입력값을 유지한다.
- 새로운 AI 분석을 요청한다.
- 새 결과가 정상 생성되면 기존 결과를 교체한다.

---

## 4) 기능 명세

### 4.1 사용자 입력
사용자가 다음 3가지 정보를 입력한다.
- 전공
- 희망 직무
- 현재 준비 상황

현재 준비 상황은 여러 줄 입력을 허용한다.

### 4.2 입력값 검증
분석 전에 다음을 검사한다.
1. 필수값 입력 여부
2. 최소 글자 수
3. 최대 글자 수

권장 기준:
- 전공: 2~50자
- 희망 직무: 2~50자
- 현재 준비 상황: 10~1,000자

검증 실패 시 AI 분석을 요청하지 않는다.

### 4.3 AI 분석
검증을 통과한 입력값을 AI에 전달한다.

AI가 생성해야 하는 결과:
1. 희망 직무의 핵심 역량 5~7개
2. 사용자에게 부족한 역량
3. 부족하다고 판단한 이유
4. 추천 준비 항목 최소 3개
5. 각 항목의 우선순위
6. 각 항목의 추천 이유
7. 각 항목의 구체적인 준비 방법
8. 사용자가 바로 실행할 수 있는 다음 행동

AI는 단순 직무 정보 나열이 아니라 사용자의 현재 상태를 고려한 준비 순서를 제안한다.

### 4.4 결과 검증
AI 응답 표시 전에 다음 조건을 검사한다.
- 핵심 역량 존재
- 부족한 역량 존재
- 추천 준비 항목 3개 이상
- 각 추천 항목의 순서 존재
- 각 추천 항목의 준비 방법 존재

조건을 충족하지 못하면 정상 결과로 표시하지 않고 재시도한다.

### 4.5 로딩 및 재시도
- 요청 직후 로딩 상태를 표시한다.
- 요청 중 중복 요청을 방지한다.
- 20초 이내 응답이 없으면 타임아웃 처리한다.
- 실패/타임아웃 시 다시 요청할 수 있게 한다.
- 입력값은 유지한다.

### 4.6 데이터 처리 범위
- 로그인/회원가입 없음
- 결제/구독 없음
- 사용자 입력 및 결과 DB 저장 없음
- 외부 채용 플랫폼/포트폴리오/캘린더 연동 없음
- 실시간 채용공고 분석 없음

---

## 5) 예외 처리

모든 예외 상황에서도 **화면이 깨지거나 사용자가 입력한 내용을 다시 작성해야 하지 않도록** 한다.

### 5.1 필수 입력 누락
**상황:** 전공, 희망 직무, 현재 준비 상황 중 하나 이상이 비어 있음.

**처리:**
- AI 분석을 요청하지 않는다.
- 비어 있는 필드 아래에 빨간색 오류 메시지를 표시한다.
- 다른 입력값은 유지한다.
- 입력하면 오류 메시지를 제거한다.

**메시지:** `필수 정보를 입력해주세요.`

### 5.2 입력값이 너무 짧은 경우
**상황:** 전공/희망 직무 2자 미만 또는 준비 상황 10자 미만.

**처리:** AI 요청을 하지 않고 해당 필드에 기준을 안내한다. 기존 입력값은 유지한다.

**메시지:** `전공과 희망 직무는 2자 이상, 준비 상황은 10자 이상 입력해주세요.`

### 5.3 입력값이 너무 긴 경우
**상황:** 전공/희망 직무 50자 초과 또는 준비 상황 1,000자 초과.

**처리:** 입력 내용을 삭제하지 않고 초과한 필드에 허용 범위를 안내한다. AI 요청을 하지 않는다.

**메시지:** `전공과 희망 직무는 50자 이내, 준비 상황은 1,000자 이내로 입력해주세요.`

### 5.4 AI 요청 실패
**상황:** AI 서비스 오류, 요청 처리 오류 등으로 분석 요청이 실패함.

**처리:**
- 로딩을 종료한다.
- 전체 화면을 오류 화면으로 바꾸지 않는다.
- 입력값을 유지한다.
- `다시 시도` 버튼을 제공한다.

**메시지:** `분석에 실패했습니다. 잠시 후 다시 시도해주세요.`

### 5.5 AI 응답이 20초 이상 걸리는 경우
**상황:** 분석 요청 후 20초가 지나도 결과가 도착하지 않음.

**처리:**
- 현재 요청을 타임아웃 처리한다.
- 무한 로딩을 종료한다.
- 다시 요청할 수 있는 상태로 변경한다.
- 입력값을 유지한다.

**메시지:** `분석에 시간이 오래 걸리고 있습니다. 다시 시도해주세요.`

### 5.6 AI 응답 형식이 잘못된 경우
**상황:** 핵심 역량, 부족한 역량, 추천 준비 항목 등 필수 영역이 누락되거나 구조를 해석할 수 없음.

**처리:**
- 불완전한 결과를 사용자에게 표시하지 않는다.
- 오류 메시지를 표시한다.
- `다시 분석하기`를 제공한다.
- 입력값을 유지한다.

**메시지:** `결과를 완성하지 못했습니다. 다시 분석해주세요.`

### 5.7 추천 준비 항목이 3개 미만인 경우
**상황:** AI가 추천 준비 항목을 1~2개만 반환함.

**처리:** 정상 결과로 표시하지 않고 다시 분석할 수 있도록 한다.

**메시지:** `추천할 준비 항목이 충분하지 않습니다. 다시 분석해주세요.`

### 5.8 직무와 관계없는 결과가 반환되는 경우
**상황:** 사용자가 입력한 희망 직무와 관련성이 낮거나 일반적인 취업 조언만 반환됨.

**처리:**
- 해당 결과를 정상 결과로 표시하지 않는다.
- 입력값을 수정하거나 다시 분석할 수 있도록 한다.
- 기존 입력값을 유지한다.

**메시지:** `직무에 맞는 분석 결과를 만들지 못했습니다. 희망 직무와 준비 상황을 조금 더 구체적으로 입력한 후 다시 시도해주세요.`

### 5.9 네트워크 오류
**상황:** AI 분석 요청 중 네트워크 연결이 끊어짐.

**처리:**
- 로딩을 종료한다.
- 입력값을 유지한다.
- 기존 결과가 있다면 기존 결과도 유지한다.
- 재시도 버튼을 표시한다.

**메시지:** `네트워크 연결을 확인한 후 다시 시도해주세요.`

### 5.10 분석 중 중복 요청
**상황:** AI 분석 중 사용자가 분석 버튼을 여러 번 누름.

**처리:**
- 분석 중 버튼을 비활성화한다.
- 동일 요청이 여러 번 실행되지 않게 한다.
- 별도 오류 메시지는 표시하지 않는다.
- 분석 종료 후 버튼을 다시 활성화한다.

### 5.11 오류 처리 공통 원칙
1. 입력값을 임의로 삭제하지 않는다.
2. 화면 전체를 새로고침하거나 초기화하지 않는다.
3. 문제와 해결 방법을 짧게 안내한다.
4. 가능한 경우 `다시 시도` 또는 `다시 분석하기`를 제공한다.
5. 무한 로딩을 허용하지 않는다.
6. 불완전한 AI 결과를 정상 결과처럼 표시하지 않는다.
7. 오류 후 처음부터 다시 입력하지 않아도 되도록 한다.

---

## 6) 완료 조건

### 핵심 기능
- [x] 전공 입력 가능
- [x] 희망 직무 입력 가능
- [x] 현재 준비 상황 여러 줄 입력 가능
- [x] 입력값 검증 가능
- [x] 정상 입력 시 AI 분석 요청 가능
- [x] 분석 중 로딩 표시
- [x] 정상 AI 결과가 20초 이내 표시
- [x] 핵심 역량 5~7개 내외 표시
- [x] 부족한 역량 표시
- [x] 추천 준비 항목 최소 3개 표시
- [x] 추천 항목에 우선순위 표시
- [x] 추천 항목에 추천 이유 표시
- [x] 추천 항목에 구체적인 준비 방법 표시
- [x] 추천 항목을 1단계 → 2단계 → 3단계 순서로 확인 가능
- [x] 결과 다시 분석 가능

### 예외 처리
- [x] 빈 입력 시 오류 메시지 표시
- [x] 너무 짧은 입력 시 최소 글자 수 안내
- [x] 너무 긴 입력 시 최대 글자 수 안내
- [x] AI 요청 실패 시 화면이 깨지지 않음
- [x] 20초 이상 지연 시 무한 로딩이 발생하지 않음
- [x] AI 응답 형식 오류 시 불완전한 결과를 표시하지 않음
- [x] 추천 항목이 3개 미만이면 정상 결과로 처리하지 않음
- [x] 직무와 관계없는 결과에 대해 재분석 가능
- [x] 네트워크 오류 발생 시 입력값 유지
- [x] 분석 중 중복 요청 방지

### 범위 제한
- [x] 로그인/회원가입 기능 없음
- [x] 결제/구독 기능 없음
- [x] 사용자 정보를 DB에 저장하지 않음
- [x] 실시간 채용공고 분석 기능 없음
- [x] 이력서/자소서 작성 및 첨삭 기능 없음
- [x] 채용공고 추천 및 지원 관리 기능 없음
- [x] 장기 성장 추적 및 대시보드 기능 없음
- [x] 외부 서비스 연동 없음
